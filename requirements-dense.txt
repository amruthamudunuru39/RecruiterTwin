# Optional dense embedding layer for Stage-2 re-ranking (recommended locally).
# Pulls torch (~700MB+), so it is kept OUT of the base requirements.txt to keep
# free-tier cloud sandbox builds fast. Install on top of the base requirements:
#
#     pip install -r requirements.txt -r requirements-dense.txt
#     python scripts/download_model.py   # one-time, downloads all-MiniLM-L6-v2 to ./models/
#
# If this is not installed the pipeline logs a warning and falls back to the
# BM25 + TF-IDF blend automatically — nothing breaks.
sentence-transformers>=3.0
