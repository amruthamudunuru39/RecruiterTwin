scikit-learn>=1.3
pandas>=2.0
streamlit>=1.30
plotly>=5.18
rank-bm25>=0.2.2
ijson>=3.2.3            # streaming JSON-array parser — no full-file load for large uploads
psutil>=5.9            # optional RSS guard in the Streamlit upload path
# Dense embedding layer for Stage-2 re-rank (pulls torch, CPU-only). Required to
# reproduce the submission — the ranker runs the dense MiniLM blend and finishes
# the 100K pool in ~180s (< 5-min budget). The all-MiniLM-L6-v2 model is committed
# under ./models, so no download is needed at ranking time. If this package is ever
# absent the pipeline auto-falls back to the BM25 + TF-IDF blend (different scores).
sentence-transformers>=3.0
