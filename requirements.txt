scikit-learn>=1.3
pandas>=2.0
streamlit>=1.30
plotly>=5.18
rank-bm25>=0.2.2
ijson>=3.2.3            # streaming JSON-array parser — no full-file load for large uploads
psutil>=5.9            # optional RSS guard in the Streamlit upload path
# Optional dense embedding layer (pulls torch ~700MB) lives in requirements-dense.txt.
# The pipeline auto-falls back to the BM25 + TF-IDF blend when it is not installed,
# which keeps free-tier cloud builds (e.g. Streamlit Community Cloud) fast and light.
# For the full dense re-rank locally:  pip install -r requirements-dense.txt
